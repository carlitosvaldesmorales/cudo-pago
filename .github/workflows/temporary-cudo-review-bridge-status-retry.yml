name: Temporary retry CUDO Review Bridge Status

on:
  push:
    paths:
      - '.github/workflows/temporary-cudo-review-bridge-status-retry.yml'

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      CUDO_GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.CUDO_GOOGLE_OAUTH_CLIENT_ID }}
      CUDO_GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.CUDO_GOOGLE_OAUTH_CLIENT_SECRET }}
      CUDO_GOOGLE_REFRESH_TOKEN: ${{ secrets.CUDO_GOOGLE_REFRESH_TOKEN }}
    steps:
      - name: Read bridge status only
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, os, urllib.parse, urllib.request
          sid='1NI73jgr_PFvrsHy27ejE_kLbTWwLbM1Y8Nyc6mRaWlcCaLlZrpEB3Ud8'
          body=urllib.parse.urlencode({'client_id':os.environ['CUDO_GOOGLE_OAUTH_CLIENT_ID'],'client_secret':os.environ['CUDO_GOOGLE_OAUTH_CLIENT_SECRET'],'refresh_token':os.environ['CUDO_GOOGLE_REFRESH_TOKEN'],'grant_type':'refresh_token'}).encode()
          with urllib.request.urlopen(urllib.request.Request('https://oauth2.googleapis.com/token',data=body,headers={'Content-Type':'application/x-www-form-urlencoded'})) as r: access=json.load(r)['access_token']
          req=urllib.request.Request(f'https://script.googleapis.com/v1/scripts/{sid}:run',data=json.dumps({'function':'cudoReviewEventBridgeStatus','devMode':True}).encode(),headers={'Authorization':'Bearer '+access,'Content-Type':'application/json'},method='POST')
          with urllib.request.urlopen(req) as r: d=json.load(r)
          result=((d.get('response') or {}).get('result') or {})
          print(json.dumps({'token_configured':bool(result.get('token_configured')),'trigger_count':result.get('trigger_count')},indent=2))
          if not result.get('token_configured'): raise SystemExit(2)
          PY
