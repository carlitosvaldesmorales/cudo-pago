const { google } = require('googleapis');
function requiredEnv(name){const value=process.env[name];if(!value||!String(value).trim())throw new Error(`${name} no está configurado`);return String(value).trim();}
function buildOAuthClient(){const auth=new google.auth.OAuth2(requiredEnv('CUDO_GOOGLE_OAUTH_CLIENT_ID'),requiredEnv('CUDO_GOOGLE_OAUTH_CLIENT_SECRET'));auth.setCredentials({refresh_token:requiredEnv('CUDO_GOOGLE_REFRESH_TOKEN')});return auth;}
(async()=>{const deploymentId=requiredEnv('CUDO_APPS_SCRIPT_DEPLOYMENT_ID');const auth=buildOAuthClient();const script=google.script({version:'v1',auth});const execution=await script.scripts.run({scriptId:deploymentId,requestBody:{function:'processPendingReviewsQA',devMode:false}});if(execution.data.error)throw new Error(JSON.stringify(execution.data.error.details||execution.data.error));const result=execution.data.response&&execution.data.response.result;if(!result||typeof result!=='object')throw new Error('processPendingReviewsQA no devolvió resumen');if(Number(result.error||0)>0)throw new Error(`La cola terminó con ${result.error} error(es): ${JSON.stringify(result)}`);console.log(JSON.stringify({ok:true,result},null,2));})().catch(err=>{console.error('CUDO Review Queue ERROR:',err.message||err);process.exit(1)});
